# Web App

Demo UI: user enters URL, receives phishing/benign label, confidence, explanation, and attention heatmap.

- **frontend/** — React app or static HTML/JS calling the FastAPI backend.
- **demo/** — Optional minimal demo script or static prototype.
