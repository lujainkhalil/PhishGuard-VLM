[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: slower tests that compose multiple modules (default: run with all tests)
    requires_torch: needs PyTorch installed
    network: needs outbound internet (live HTTP crawls)
